language: node_js
sudo: false
node_js: stable
addons:
  apt:
    sources:
      - google-chrome
    packages:
      - google-chrome-stable
cache:
  directories:
  - node_modules
install:
  - npm install -g polymer-cli
  - npm install
script:
  - xvfb-run polymer test --npm -l chrome
dist: trusty
