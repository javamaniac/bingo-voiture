language: node_js
sudo: false
node_js: stable
addons:
  apt:
    sources:
      - google-chrome
    packages:
      - google-chrome-stable
cache:
  directories:
  - node_modules
install:
  - npm install
script:
  - xvfb-run wct --npm -l chrome
dist: trusty
